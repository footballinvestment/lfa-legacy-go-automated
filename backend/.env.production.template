# LFA Legacy GO - Production Environment Template
# Copy this file to .env.production and fill in the values

# ===== REQUIRED PRODUCTION SETTINGS =====

# Environment
ENVIRONMENT=production
DEBUG=false
PORT=8080

# ===== SECURITY (CRITICAL) =====

# Admin Credentials - MUST BE SET FOR PRODUCTION
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_123!
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_FULL_NAME="System Administrator"

# JWT Configuration - MUST BE SET FOR PRODUCTION
JWT_SECRET_KEY=CHANGE_THIS_TO_SECURE_RANDOM_STRING_64_CHARS_LONG_abcdef123456789
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# ===== DATABASE =====

# PostgreSQL (Recommended for production)
DATABASE_URL=postgresql://username:password@localhost:5432/lfa_legacy_go
# Or Cloud SQL for Google Cloud deployment:
# DATABASE_URL=postgresql://username:password@/lfa_legacy_go?host=/cloudsql/PROJECT:REGION:INSTANCE

# Connection Pool Settings
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_POOL_PRE_PING=true

# ===== REDIS CACHE =====

# Redis Configuration (Optional but recommended)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=optional_redis_password

# ===== SECURITY SETTINGS =====

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# CORS Origins (Comma-separated list)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===== LOGGING & MONITORING =====

# Logging Level
LOG_LEVEL=INFO
JSON_LOGGING=true

# Performance Monitoring
ENABLE_METRICS=true
METRICS_PORT=9090

# ===== EXTERNAL SERVICES =====

# Email Configuration (for notifications)
EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_FROM_ADDRESS=noreply@yourdomain.com
EMAIL_FROM_NAME="LFA Legacy GO"

# Cloud Storage (Optional)
CLOUD_STORAGE_BUCKET=your-bucket-name

# ===== GOOGLE CLOUD DEPLOYMENT =====

# Google Cloud Project
GOOGLE_CLOUD_PROJECT=your-project-id

# Cloud SQL Connection
CLOUD_SQL_CONNECTION_NAME=project:region:instance

# ===== DEVELOPMENT OVERRIDES =====
# These settings override defaults for specific environments

# Development database (uncomment if needed)
# DATABASE_URL=sqlite:///./lfa_legacy_go_dev.db

# Local development CORS (uncomment if needed)  
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ===== SECURITY CHECKLIST =====
# Before deploying to production, ensure:
# 
# ✓ ADMIN_PASSWORD is changed from template
# ✓ JWT_SECRET_KEY is a secure random string (64+ chars)
# ✓ DATABASE_URL points to PostgreSQL (not SQLite)
# ✓ CORS_ORIGINS contains only your domains
# ✓ All passwords are strong (8+ chars, mixed case, numbers, symbols)
# ✓ DEBUG=false for production
# ✓ ENVIRONMENT=production
# ✓ SSL/HTTPS is enabled (handled by deployment platform)
#
# Generate secure keys with:
# python -c "import secrets; print(secrets.token_urlsafe(64))"